const temCarteira = false
const temBoaVisao = false
const isTired = true

if (temCarteira || temBoaVisao || isTired) {
    console.log('Ela pode dirigir')
} else {
    console.log('Não pode dirigir')
}
